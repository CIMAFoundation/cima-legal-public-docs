name: Publish Manifest

on:
  push:
    branches: ['main']
    paths:
      - 'legal-docs/**'
      - 'scripts/**'
      - 'package.json'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run build:manifest
      - name: Commit normalized manifest
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add legal-docs/manifests/latest.json
          git commit -m "chore: normalize latest manifest"
          git push
      - name: Sync to GitHub Pages repo
        run: npm run sync:pages
        env:
          GITHUB_TOKEN: ${{ secrets.CIMA_MACHINE_USER_API_TOKEN }}
          PAGES_REPO: ${{ vars.PAGES_REPO }}
